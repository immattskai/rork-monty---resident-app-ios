# Lift Ask Monty above the header shadow

**Change**

- Raise the Ask Monty card so it sits on top of the header row's drop shadow instead of being slightly tucked beneath it.
- The rest of the scrolling content (tiles, announcements, etc.) stays exactly where it is — only the card the user pointed at moves up the stacking order.
- Visual position on screen does not change; only the layering does, so the shadow no longer falls across the top edge of Ask Monty.

